# 11 App 图标与 A4 计划同步说明

文档版本：v0.3  
日期：2026-05-01  
来源 PDF：本机 `A4-两周复习进度规划表.pdf`

## 1. 图标设计

图标主题：手绘 `XYX + 学` 参考图提取。

视觉元素：

- `XYX`：来自用户手绘稿，含义为“学呀学”。
- `学`：使用手绘笔迹提取，而不是系统字体，保留个人项目辨识度。
- 白色背景：采用接近常规软件图标的干净白色，避免使用照片里的发黄纸色。
- 蓝色主体：从手绘图中提取马克笔蓝，当前提取值为 `#265CA3`。
- 轻微阴影：只用于让蓝色笔迹在白底上更清晰，不做复杂装饰。

落地文件：

- `FinalPilotApp/Assets.xcassets/AppIcon.appiconset/`
- `tools/generate_icon_from_reference.py`
- `design/reference_icon/FinalPilot_reference_extracted_icon.png`

说明：当前图标由参考图提取脚本生成。脚本运行时读取本机参考图，但仓库不提交原始照片，只提交处理后的 AppIcon 资产和预览图。

设计复盘讲法：

`XYX` 可以解释为“学呀学”。这版图标直接来自手绘草图，表达这个项目不是模板化 App，而是围绕真实考试压力、真实复习资料和真实里程碑展示做出来的个人学习工具。蓝色保留手写稿的能量，白底让它更像正式 iOS 软件图标。

## 2. PDF 计划同步

已从 A4 PDF 中提取并同步到 App：

- 5/1 到 5/14 每日计划
- 四个阶段：搭骨架、补高频、真题闭环、考试切换
- C310 每日主线
- E320 每日主线
- 真题 / 输出物
- 完成打勾标准
- 每日保底规则
- C310 / E320 执行标准

落地文件：

- `FinalPilotApp/PlanView.swift`
- `FinalPilotApp/Models.swift`
- `FinalPilotApp/SeedData.swift`
- `data/a4_sprint_plan_seed.json`

## 3. 手机上怎么用

在 Xcode 中运行到模拟器或真机后：

1. 打开 FinalPilot。
2. 点击底部 `计划`。
3. 顶部查看三门考试日期：C310 5/13、E320 5/14、C315 5/26。
4. 使用阶段筛选查看 `搭骨架`、`补高频`、`真题闭环`、`考试切换`。
5. 每天按卡片完成 C310、E320、输出物和勾选项。
6. 如果当天有临时事务或状态差，执行 `每日保底`，不要补偿性熬夜。

## 4. 后续可迭代

下一版可以把计划卡片变成可勾选状态，并接入本地持久化：

- 每日计划完成状态保存到 SwiftData。
- 当天自动定位到今日日期。
- 考后自动把 C315 提升为主复习课程。
- 将 A/B/C 档做成切换器，自动压缩当天任务。
